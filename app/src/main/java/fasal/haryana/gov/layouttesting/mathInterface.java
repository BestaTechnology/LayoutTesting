package fasal.haryana.gov.layouttesting;

public interface mathInterface extends secondInterface {

    /*by default all methods are abstract method in interface*/

    /*Interface supports multiple inheritance.
     */
    /*by default all variables are public static final in interface*/

   /*An interface can extend another Java interface only.*/
    public static final int aa=10;
    void subtracct(int x, int y);


    @Override
    void addnum();
}
